# DEFECT REGISTER — ranked launch blockers

Every defect: observed failure → earliest incorrect pipeline stage → one owner → one
independent reviewer → smallest reproduction → universal fix → regression → real-run
proof → adversary sign-off. A builder never closes their own defect. Severity is
CRITICAL (invalidates answers or honesty), HIGH (materially wrong world/trace),
MEDIUM, LOW.

## Open

| ID | Sev | Defect (observed, with artifact) | Earliest stage | Owner / Reviewer | Req |
| --- | --- | --- | --- | --- | --- |
| FD-17 | CRITICAL | Live run artifacts/phase2/geopolitical2 (commit bc20aa0) published `probability: 1.0000` labeled `weighted_simulated_trajectories` with only 0.25 of mass resolved (1 branch YES, 3 UNRESOLVED, 0.75 unresolved). The point estimate is conditional on a MINORITY of branch mass over ungrounded symmetric-ignorance weights; bounds [0,1] and status partially_resolved are honest but the headline number is not. FI-2's "ungrounded weights that DISAGREE" test does not fire when the others are unresolved rather than opposed. | aggregation/publication | forecast-integrity / forensic auditor | FI-2, FI-3 |
| FD-18 (CLOSED 89e04c0, realism-adversary) | HIGH | 8 of 12 wake-ups flagged repeated_without_new_information. Adversary decomposition: 4 are legitimate process_opportunity (a scheduled meeting happens whether or not news arrived) and 4 are duplicate deadline_reached waking the same actor about the same node the same day after it already answered, with no state change — the real FD-7 're-signaled 3x' shape. Fix: cancel a node's deadline for a participant that already exercised the opportunity; split the counter by calendar-driven vs information-driven cause or ACT-8 flags correct behavior ~50% of the time and gets ignored. | scheduling/wake rules | runtime-impl-2 / realism-adversary | ACT-8, TMP-3 |
| FD-19 (CLOSED 89e04c0, realism-adversary) | HIGH | TMP-4/FD-7 class SURVIVES: uncertainty.py:163-176 sets release_at=min(releases), merging every uncertainty in a branch into ONE release moment. Proven on the real engine (probe_release_merge.py): variables dated 06-01 and 06-20 both fire 06-01 — 19 simulated days of actors deciding on information that does not exist yet; a null-timing variable is released at another's date, which engine.py:405-407's own docstring forbids. | scenario seeding | runtime-impl-2 / realism-adversary | TMP-4 |
| FD-20 (CLOSED 89e04c0, realism-adversary) | HIGH | temporal_report messages block counts three different universes: create_event (terminal result_recorded, never propagated) inflates messages.sent by branch_count on EVERY run (geopolitical2 reports sent:4, truly 0); update_commitment dropped though it generates deliveries, so a missed commitment is invisible; sent counts events while delivered/noticed/missed count (event,actor) pairs ("16 of 5 delivered"). The M1 regression asserts the wrong invariant. | run reporting | runtime-impl-2 / realism-adversary | TMP-2, COM-2 |
| FD-21 (CLOSED 89e04c0, realism-adversary) | MEDIUM | process_updates counts branch-hypothesis seeds on the deferred path (only the immediate path stamps branch_conditions); geopolitical2's process_updates:4 is truly 0. Same blind spot at replaycore.py:335,356. same_timestamp_communications false-positives from the same widened kind set. | run reporting / replay | runtime-impl-2 / realism-adversary | TMP-2, TMP-3 |
| FD-22 | MEDIUM | replaycore.COMMUNICATION_EVENT_KINDS was not narrowed alongside the report, so communications.jsonl still extracts terminal result_recorded lines as communications (geopolitical3: its 1 row IS the terminal line; geopolitical2: all 4 rows are). The two artifacts now disagree and the tying test was deleted rather than retargeted. Not blocking: the exit-review counter is honest, stray rows are self-evidently non-messages (null sender, no recipients), and no rendered surface aggregates them. | trace extraction | obs-trace / realism-adversary | COM-2 |
| FD-23 | LOW | Release dated beyond the horizon is silently scheduled and dropped — neither refused nor reported (inert today: enforce_outcome_is_produced refuses uncertainties writing terminal-read fields). Two uncertainties writing one field at one moment resolve by spec order, silently masking one branch condition (pre-existing, deterministic). _release_order's docstring advertises naive/aware safety its comparison sites lack. | scenario seeding | runtime-impl / realism-adversary | TMP-1 |
| FD-24 | CRITICAL | Tesla recompile at c52cf19 (artifacts/phase3/tesla) produced a multi-stage world whose stocks go NEGATIVE: inventory -48000/-65866/-45564/-63430 and order_backlog -56000/-73866 across all four branches. The delivery process subtracts delivery_rate_weekly from inventory with no constraint that you cannot deliver what you do not have, and no floor at zero. A physical stock modeled without a non-negativity constraint is not an operational system — the multi-stage requirement (D4/CWF-3) is met in FORM while the arithmetic is impossible. **AMENDED by CW-J (causal-adversary): the CTO's proposed fix — map stocks onto the runtime's conserved RESOURCE machinery — does NOT close this.** The runtime does not enforce conservation for resources; the ACTOR EXECUTOR does. `world.py:392-407` (transfer_resource, consume_resource) subtracts with no clamp and no refusal and will produce a negative resource. The non-negativity check lives in `effects.can_apply` (effects.py:109-127), called from exactly three sites — executor.py:205, executor.py:289 (actor action paths) and novel.py:118. External-process occurrences apply effects at `engine.py:1001-1002` with NO feasibility check at all. Tesla's negative inventory was written by an operational process; had that identical drawdown been a consume_resource against a ResourceSpec it would still have gone negative. Correct fix: the invariant moves to the STATE-TRANSITION boundary (one guard wherever world state changes — process occurrence, actor action, uncertainty release), and it is a CLAMP WITH RECORDED SHORTFALL, not an exception — the shortfall is causal information (the backlog grows; that is the world working correctly), and must be readable by downstream conditions and the replay core. Adversary repro S8/K6: recharge log driven to -99,300 acre-feet, ACCEPTED, review PASSES. | state-transition boundary (invariant on the wrong path) | world-compiler-impl / causal-adversary | CWF-2, CWF-3 |
| FD-25 (WIDENED by CW-D, causal-adversary) | CRITICAL | Same run: a WEEKLY delivery rate fired only TWICE in a ~10-week quarter (occurrences dated 07-28 and 09-30 only), so cumulative_deliveries = rate x 2 (56,000 / 73,866) and every branch resolved NO. **The original filing was under-scoped.** The defect is not "the cadence under-counted" but "the occurrence count is a free parameter that can straddle the threshold in EITHER direction, and the world-level moment check cannot see it." Adversary repro (attacks3.py K3, clean e079577): identical world, `weekly` cycle typed 8x -> 840 vs typed 10x -> 990 against threshold 900 — terminal flips NO->YES on how many times the planner pressed enter; both ACCEPTED by the static validator and both PASS review. D3 cannot see it because D3 only iterates `plan.uncertainties`. Compounding: `world_review.py:615` counts distinct moments WORLD-WIDE, not per process — which is exactly how phase3/tesla/world_review.json reported "the world acts at 10 distinct moments" while compiled_world.json beside it has 2 occurrences per process. A correctly-scheduled process launders an under-scheduled one. No cadence/period concept exists anywhere in src/ (grep-verified). | semantic planning (process cadence) | world-compiler-impl / causal-adversary | CWF-2, TMP-1 |
| FD-26 (**MISDIAGNOSED** — corrected by CW-L, causal-adversary) | HIGH | ORIGINAL FILING WAS WRONG: compiled_world.json **does** carry the justification, at `representation_record.zero_actor_justification`, with full text and four claim ids. The `null` I filed against is a top-level key that was never written. **The real defect in that artifact is worse:** `representation_record.included` is `[]` AND `world_spec.entities` is `[]`. The Tesla world contains no occupant at all — so CWF-1's "every included entity answers the five questions" is satisfied VACUOUSLY, and no gate anywhere requires a world to contain the subject it is about. Fix: a non-empty-record check for any non-factual terminal, plus reconciliation of `plan.entities` against `world_spec.entities`. | compile gates (vacuous satisfaction) | world-compiler-impl / causal-adversary | CWF-1, CWF-5 |
| FD-27 | HIGH | Same run: world_review returned SEVEN blocking failures (what_process_produces_outcome, process_is_represented, material_components_missing, decorative_actors, material_actor_compressed_away, expert_would_call_incomplete, no_intermediate_production_state) and the run simulated and PUBLISHED anyway ("recompiled; the world simulated is not the world reviewed here"), after 3 repair rounds. A blocking review that ends in publication is not a gate. Note the new mechanical check no_intermediate_production_state fired correctly and was still overridden. | forecast assembly / repair loop | integration-1 + world-compiler-impl / causal-adversary | CWF-6, FI-3 |
| FD-46 | HIGH | **`derive_state_diffs` produces nothing for record worlds, so `state_diffs.jsonl` is EMPTY for every actor world of that shape.** Measured by reliability-impl: a multiparty run with 5 `append_record` events yields 0 state_diff rows — the function replays only `_apply_field_op`. `tracing.py` advertises that artifact under OBS-2/§12 as "every state diff". Compounding it, `terminal_field_reads` returns `()` for a content-predicated terminal (it walks only `field` ops), so `terminal_read_fields_affected` is uniformly empty too. A record append genuinely has no `state_before`/`state_after` field shape, so this is an artifact-SCHEMA change rather than a bug fix — the diff row must gain a record form. | trace extraction / artifact schema | reliability-impl / obs-reviewer | OBS-2, OBS-4 |
| FD-47 | MEDIUM | **`render_dossier` shows no record content.** Measured: the dossier for a run decided entirely by recorded positions contains neither the collection name `positions` nor the value `hold`. The human-readable page a reviewer opens shows the verdict and the counterfactual table but no evidence of what was actually recorded — i.e. for an actor world it omits the thing that decided the answer. Partially mitigated by the new `records_read` (`terminal_evaluations.jsonl`) and `final_records` (`forensic_verdict.json`), which the dossier already links. | forensic rendering | reliability-impl / obs-reviewer | OBS-6 |
| FD-48 | MEDIUM | **`_apply_count_op` merges the collection and event-type namespaces into one `counts` dict, which the engine treats as SEPARATE** (`prompts.py:372`). Live evidence: in the BoE run's YES branch `bailey_s_public_signal_of_support` is both an event type (2 `create_event`) and a collection (2 `append_record`); the replay reports **4** where the live world reports **2**, and `event_type_counts: 4` is in the shipped artifact. It never changed an answer — the terminal is `> 0` and both values clear it — so the completed audits stand. `counts` was left untouched deliberately so those audits reproduce byte-for-byte; fixing this must be a separate, deliberate change with its own before/after artifact diff. | replay core | reliability-impl / forecast-integrity | OBS-4, FI-4 |
| FD-49 | MEDIUM | **`reevaluate_terminal`'s `records=`/`events=` are OPTIONAL, so a caller that forgets them silently gets the old blank-placeholder behaviour** — the exact FD-42 wrong answer, reachable by omission. Kept optional because making them required would break `test_observability.py` and `test_responsibility.py`, which the implementing agent did not own. This is the FD-45 shape once more: a path that declines to read content is indistinguishable from one that read it and found none. Revisit once every call site passes a full `ReplayState`; until then the default is a footgun aimed at the defect we just closed. | replay core (API shape) | reliability-impl / causal-adversary | OBS-4, FI-4 |
| FD-45 | CRITICAL | **A mechanical check that CRASHES is indistinguishable from one that ran and approved.** `world_review.py:1351-1354` — `try: mechanical = mechanical_world_checks(compiled, evidence) / except Exception: mechanical = ()`, commented *"a check that cannot run blocks nothing."* Any fault anywhere in the five operational-depth checks silently yields ZERO findings for every non-settled-record world in the system, including two CRITICALs (`terminal_set_in_one_step`, `multiplier_lacks_evidence`), and the run publishes. **This is the FD-34 bypass reopened from a different direction**: FD-34 was a PROVIDER outage laundering mechanical facts; this is a CODE fault doing the same thing, and unlike FD-34 it is invisible to the suite — the resulting failures read as assertion failures about severities rather than as "the function raised". Demonstrated live: integration-1 caught a transient undefined-name in the FD-33 refactor and observed `mechanical_world_checks(...)` raise `NameError` while `review_world(...).findings` returned `0 findings, blocking = []` — a ferry world that must produce five blocking findings produced zero. That specific NameError is now fixed, **but the swallow that made it silent is still there and will make the next one silent too.** The blanket `except Exception` was justified by a real incident (a live BoE run died in the review's own summary helper because a compiled `at` is an ISO string, not a datetime) — that justification is sound for the MODEL-OPINION half and wrong for the mechanical half. Fix, by the same principle already ruled for FD-34: a mechanical pass that cannot complete must emit a BLOCKING finding naming the fault, never an empty tuple. A gate that cannot run must never read as a gate that ran and approved. | world review (fault handling) | world-review-impl / causal-adversary | CWF-6, FI-3 |
| FD-43 | HIGH | **A suppressed run tells a reader we declined — and leaks the number we declined to publish.** Verified by forecast-integrity against geopolitical2's real branch table. The CLI renders `Simulation probability: —`, a snake_case source token, `[0.0000, 1.0000]`, and then `p_before=— -> p_after=1.0000 calibrated=False` plus a counterfactual note ending *"...instead of 1.0000."* **The suppressed 1.0000 is rendered TWICE in the CLI summary; the reason is rendered ZERO times** — FD-17 leaks straight back out through the integrity line. Five findings: (1) `cli.py:65-112` never reads `result.limitations` or `point_estimate_suppression_reason` (grep: zero references), so the composed, publishable reason never reaches the console — it appears once in report.md ~25 sections later in a bullet list beside boilerplate; (2) `cli.py:99-108` re-publishes the withheld figure — `p_after` is the scenario average, unlabelled, on a line that reads as a result; the note is generated by `outcomes.py:421 _counterfactual_note`; (3) **most serious** — `tracing.py:54-100 forecast_payload` carries NONE of `scenario_average`, `point_estimate_suppressed`, `point_estimate_suppression_reason`, `answer_withheld`, `validity`, `responsibility`, so the machine-readable artifact says `simulation_probability: null` and stops; `scripts/forensics.py check()` returns early on `None` and a suppressed run reconstructs silently as though nothing was published, while `viz/replay.py:99` and `viz/dossier.py:395` render `"probability": null` with no explanation; (4) **nothing computes what would make the answer KNOWABLE, so no renderer can show it** — every suppression reason has a precise computable requirement the system already holds and discards (minority mass: `resolved_mass_share = 0.25`, a further 0.25 must resolve; ungrounded disagree: the named weights must come from one of the six §15 evidence classes; straddling: `seasonal_factor_q3_over_q2` draws 0.8 -> NO, 1.05 -> YES with everything else fixed; responsibility gate: the classification's own `reason`, already composed and never rendered); (5) a suppressed run discards the finding it DID produce — BoE's *"YES in exactly 1 of 4 enumerated branches: job_market_condition=slowing AND inflation_outlook=other; no single condition separates YES from NO — it is the conjunction"* is computable from the branch table and rendered nowhere, though it is the audit's own "trust the structure, not the number" verdict and the honest product of a suppressed run. **A withheld point estimate is only a product if the reader learns what would have to be true for the answer to be knowable.** Fix order: finding 4 first (owner forecast-integrity — `KnowabilityCondition` on `ForecastResult`, `_knowability()` driven off the same `reasons` list at outcomes.py:218 so a new suppression rule cannot ship without its knowability clause), then 1+2 (integration-1), then 3 (reliability-impl + viz). | publication / rendering | forecast-integrity / causal-adversary | FI-2, REL-2 |
| FD-44 | MEDIUM | `engine.py:1871` sets `unresolved_reason = terminal.description or ...`, so all three unresolved geopolitical2 branches read *"OPEC+ announces an increase in crude oil production quotas on or before 2026-10-01."* — **the question restated as the reason it did not resolve.** The informative path already exists one branch away at `engine.py:1881` (*"terminal depends on a value the world never determined: ..."*). Escalates once FD-43 finding 4 lands, because `unresolved_reason` then becomes the load-bearing string for "what is missing". | runtime (terminal evaluation) | runtime-impl / realism-adversary | FI-2, TMP-2 |
| FD-42 (CLOSED b8bcf4a+, reliability-impl; verified independently) | CRITICAL | **`replaycore` cannot reconstruct record CONTENT, so the forensic tool has a live false-invalid.** `ReplayWorld.get_records()` (replaycore.py:420) reconstructs collection CARDINALITY and returns N *empty* dicts. A cardinality terminal replays exactly. A content-predicated one — `count('positions', equals(item('value'), 'hold')) >= 5`, which is what `_worlds.scheduled_multiparty_world` and **every actor world in this repo** uses — evaluates over blanks, returns 0, and answers NO with full confidence. Found by forecast-integrity probing the D6 gate against a real `run()`: the genuine YES branch of `_split_world` replayed as NO, and a naive gate would have called a SOUND run INVALID. Two consequences, the second worse: (1) the D6 responsibility gate cannot evaluate mandatory counterfactuals for those terminals — `classify_responsibility` now detects the shape (`sum`/`values` always; `count`/`exists`/`event_count` with a where-predicate) and refuses with `error="replay_cannot_reconstruct_record_content"`, classification INVALID; failing closed is right, but **integration-1 must NOT wire `responsibility=` until this is fixed** or every actor world withholds its answer (`world_review_blocking=` is unaffected and can be wired now). (2) **`scripts/forensics.py` shares the blind spot and does not know it** — it calls the same `reevaluate_terminal`, so a content-predicated run reconstructs as NO and is reported as a CRITICAL `simulation_probability` mismatch → verdict FORENSICALLY_INVALID against a perfectly sound run. The three audited runs happened not to use that shape, which is the only reason the forensic audit came back clean. Fix is small and is a bug, not a restructure: `append_record` payloads already carry `{collection, key, value, extra}` (verified on a real ledger), so `replay_fields` can accumulate the records and `get_records` return them instead of blanks. **Blocks activation of the D6 gate.** | replay core (D7 single implementation) | reliability-impl / forecast-integrity | FI-3, FI-4, OBS-4 |
| FD-28 (CW-A) | CRITICAL | **One alias hop disarms D4 and every CWF-6 check.** `semantic_plan.py:1307` (`if len(mechanism) > 1 or (inputs & produced_elsewhere): continue`); `world_review.py:513` collects `multiplier_writers` only from writers of TERMINAL terms; `world_review.py:599` `intermediate = written_by_mechanism - field_terms`. Repro (attacks3.py K1, clean e079577): take the Tesla-shaped plan and split one change in two — `set projected = 320000 x factor` then `set recorded = projected` — and cite ONE of two factors. ACCEPTED; review PASSES all 4 checks. The review's own `_flipping_uncertainties` CORRECTLY COMPUTES that the draw flips the terminal and then `single_uncertain_multiplier_decides` reports PASS because the draw is read into the alias, not a terminal term — **the check derives the right fact and discards it.** Confirmed on the shipped GOOD fixture too (K5): the recharge world's answer is entirely decided by `winter_runoff_fraction`, `_flipping_uncertainties` says so, the check says PASS. Existing regression `test_a_draw_scaled_straight_into_the_total_is_seen_even_in_a_staged_world` only covers a draw read DIRECTLY into the total. **Highest-value single lever (adversary's ranking): make D4/CWF-6 operate on the terminal's LINEAGE CLOSURE, not its DIRECT WRITERS — `_lineage_states` already exists in semantic_plan.py and D3 already uses it correctly, which is why D3 catches the transitive driver and the review does not. Porting that closure into `world_review.mechanical_world_checks` and `_one_step_operational_errors` closes CW-A and most of CW-G with no new vocabulary.** | semantic validation + world review | world-compiler-impl / causal-adversary | CWF-3, CWF-6 |
| FD-29 (CW-B) | CRITICAL | **D3 requires BOTH straddling legs ungrounded; one citation silences it.** `semantic_plan.py:1417` — `if ungrounded(low[0]) and ungrounded(high[0]):`. Repro (attacks2.py S1): recharge world, cite `c-a3` on the 0.9 leg only → ACCEPTED, review PASSES. The docstring's rationale ("a cited value under symmetric-ignorance weights is priced as bounds") only holds if BOTH legs are cited, which is not what the code tests. One cited leg plus one invented leg IS the Tesla defect with a fig leaf, and it is admitted. This makes D3's narrowness load-bearing on D2/FI-2, which still has an open CRITICAL (FD-17). | semantic validation | world-compiler-impl / causal-adversary | CWF-4, FI-5 |
| FD-30 (CW-C) | CRITICAL | **`cited()` is an existence check masquerading as a support check.** `semantic_plan.py:1784-1787` — `return True if known is None else all(i in known for i in ids)`. Any claim id in the store grounds anything. Repro: put `c-f1` ("The Kestrel Bay ferry terminal recorded 320000 crossings") on both straddling alternatives of the AQUIFER uncertainty → ACCEPTED. This is the load-bearing predicate for D3, D5's zero-actor justification, D4's `single_multiplier_exemption` and FD-11's filler gate — **all four defeated by citing an arbitrary claim.** Second repro: `single_multiplier_exemption` + both factors citing unrelated aquifer claims → ACCEPTED. D4 also names "reviewer approval" as an exemption condition; nothing enforces it — the exemption is recorded, never adjudicated. | semantic validation | world-compiler-impl / causal-adversary | CWF-3, CWF-4, CWF-5, FI-6 |
| FD-31 (CW-E) | HIGH | **Every occurrence of a "weekly" process at ONE instant passes.** Repro (K4): all 10 infiltration cycles dated 2026-02-20 → ACCEPTED, review PASSES — the setup step supplies a second timestamp and `len(moments) > 1` is the whole test. | world review (temporal check) | world-compiler-impl / causal-adversary | CWF-6, TMP-1 |
| FD-32 (CW-F) | HIGH | **An UNKNOWN-initial accumulator is invisible to BOTH forward evaluators.** `semantic_plan.py:907` — `if amount is None or c.target not in env: continue`; `world_review.py:404` — `if isinstance(delta,(int,float)) and isinstance(current,(int,float))`. Repro (S2): recharge world with the terminal stock's initial set to UNKNOWN and its writer changed from `set` to `increase` → straddle undetected, ACCEPTED, review PASSES. **The schema pushes planners into this blind spot**: semantic_plan's rule that a precise initial quantity requires citations makes UNKNOWN the path of least resistance for a counter that starts at zero. So the prescribed fix for FD-24/FD-25 — build genuine accumulation worlds — moves plans INTO the hole. Must be handled inside the stock/flow work or it swallows the feature. | semantic validation + world review | world-compiler-impl / causal-adversary | CWF-2, CWF-4 |
| FD-33 (CW-G) | HIGH | **One actor affordance on the terminal quantity collapses the review from 5 checks to 1.** `world_review.py:477` — `if not field_terms or (action_written & terms): return (single PASS,)`; `semantic_plan.py:1288` — `if actor_writers: continue`; `semantic_plan.py:867,1098` — the D3 forward evaluator excludes affordances entirely, so an actor-written state is "undetermined" and the straddle gate declines. Repro (K2): make an actor the sole writer of the terminal total → ACCEPTED, review PASSES with 1 check. D5's decorative-actor rule cannot save this: an actor that writes a terminal state is `direct` by construction, hence automatically "material". **Placing one actor on the terminal quantity is a universal skeleton key for D3, D4 and CWF-6.** | semantic validation + world review | world-compiler-impl / causal-adversary | CWF-3, CWF-5, CWF-6 |
| FD-34 (CW-H) | HIGH | **A failed reviewer call discards the DETERMINISTIC mechanical findings.** `world_review.py:711-715` deliberately preserves them — *"The mechanical findings survive a failed model call: they were decided by the compiled world, not by the opinion that could not be obtained."* — but `api.py:957` (`review_ran = not self.review.error`) and `api.py:985` (`blocks_publication = self.review_ran and ...`) drop them at the gate. **A gateway outage converts a computed CRITICAL (`terminal_set_in_one_step`) into an advisory note and the run publishes.** The two comments contradict each other, and `tests/unit/test_causal_world_fidelity.py:976` claims these attacks "survive an unreachable reviewer" — they survive into the RECORD, not into the GATE. This is a live bypass of FD-27's fix. **CTO RULING: mechanical findings are facts about the compiled world computed with no provider; a provider outage must not launder them. `blocks_publication` must count surviving MECHANICAL blocking findings regardless of `review_ran`; only the MODEL-authored findings go advisory when the call fails.** | forecast assembly (gate) | integration-1 / causal-adversary | CWF-6, FI-3 |
| FD-35 (CW-I) | HIGH | **The 16-combination cap hides straddles behind plan width.** `semantic_plan.py:1002` — `cap: int = 16`. Repro, identical straddle differing only in how many BACKGROUND uncertainties exist: S5 (driver + 5 ungauged channels, 32 combos > cap) → ACCEPTED; S5-control (driver + 3 channels, 16 <= cap) → REFUSED `THRESHOLD_STRADDLING_UNGROUNDED_SCENARIOS`. Documented as "under-, never over-refusing" — honest, but it means a planner escapes D3 by adding uncertainties, i.e. **by looking MORE thorough.** | semantic validation | world-compiler-impl / causal-adversary | CWF-4, FI-5 |
| FD-36 (CW-K) | MEDIUM | **`terminal_sensitivity` is self-declared and only one of three values is checked.** `TERMINAL_SENSITIVITY_MISDECLARED` fires only on `immaterial_to_the_terminal`; `DEGENERATE_FILLER_ALTERNATIVE` fires only on `decides_the_terminal`. Declaring `moves_the_terminal` evades both — the adversary used exactly this in EVERY attack above. | semantic validation | world-compiler-impl / forecast-integrity | FI-6, FD-10 |
| FD-37 (CW-M) | MEDIUM | **The persisted `world_review.json` is not the final round.** artifacts/phase3/tesla/world_review.json records `disposition: "recompiled; the world simulated is not the world reviewed here"` and 10 weekly moments; compiled_world.json beside it has 2 occurrences per process. **The artifact a human auditor reads is a review of a discarded world.** (Addressed by integration-1's `_review_and_repair` + `_world_signature` fingerprinting; needs a fresh real-run artifact to close.) | forecast assembly / artifact write | integration-1 / causal-adversary | CWF-6, OBS-5 |
| FD-38 | HIGH | **The zero-actor gate is self-certifying, and the Tesla justification is REFUSED on its own store.** `_actor_admissibility_errors` checks only that two strings are non-empty and `cited()` returns true — which per FD-30 means "any id exists in the store". Nothing checks the justification ENGAGES CONTRARY CLAIMS or that cited claims are ABOUT DECISIONS. The one mechanism that can contradict a zero-actor claim, `ZERO_ACTOR_CLAIM_CONTRADICTED`, keys on `expected_participants` — supplied by the same planner that wants zero actors, and `None` in this run. Adversary's four grounds for refusing the Tesla justification: (1) the store affirmatively names a quarter-end DECISION the justification never engages — `c-a0d742f313d6`: "Q1 and Q2 are generally the slowest seasons ... **ahead of pushes for Q3 and Q4 sales**"; a "push" is discretionary commercial action and it is the single most decision-shaped claim in the store, about the exact quarter in question; (2) the supply-constraint claims (`c-68269072bae3`, `c-83b62bb575f9`) bound PRODUCTION while the question is DELIVERIES — the plan's own world concedes the difference by modeling `inventory` and `order_backlog` as bridges; a ceiling on production is an upper bound, not proof no decision moves the realized figure, and the gap between ceiling and realized IS the discretionary space; (3) `c-062a3c8a17b4` (largest order backlog since 2023) is cited to show demand is not a constraint but **argues the opposite** — with more orders than units, which units ship to which region in which quarter is a management CHOICE, and the claim strengthens the case for actors; (4) the `process_sufficiency` half is circular and cites nothing specific, the run's own review found seven blocking failures on exactly that point, and the simulated total (56,000–73,866 vs a 400,000 threshold) was nowhere near any supply ceiling — the binding constraint was the cadence bug, not physics. Minimum fix: require the justification to NAME each decision class the store mentions and say why each cannot move the terminal; require `expected_participants` to come from the RESEARCH stage, not the planner. | compile gates (zero-actor admissibility) | world-compiler-impl / causal-adversary | CWF-5, FD-9 |
| FD-39 | HIGH | **The gates evaluate the plan's ARITHMETIC but never its DIMENSIONS** — the third defect of which FD-24 and FD-25 are both instances. Tesla added `delivery_rate_weekly` (a flow, vehicles/week) directly into `cumulative_deliveries` (a stock, vehicles) with no duration term. `SemanticState.unit` is a free-text field that **nothing reads**. `SemanticValue.kind` is `literal|state|sum|product` — a product of a rate and a count is structurally indistinguishable from a product of two scalars, so **rate x duration is the one causal form the grammar cannot express AS SUCH.** A unit-consistency check over the `unit` field the schema already carries would have caught the rate-into-stock addition at validation time, is cheaper than either the conservation or recurrence work, and is the check that GENERALIZES past this one failure. Adversary ranks this above the conservation fix on cost-effectiveness; CTO concurs. | semantic schema + validation | world-compiler-impl / causal-adversary | CWF-2, CWF-4 |
| FD-40 | HIGH | **CWF-8's 25 regressions are green and EVERY attack above passes them.** The suite proves the gates fire on the shapes they were written from; it contains no EVASION class. CWF-8 must not move until the suite has, per gate, at least one plan that is the same defect in different syntax. K1, K2, K3, K4, S1, S2, S5, S8 are eight ready-made cases in the existing invented domains (repro scripts: scratchpad/attacks2.py, attacks3.py). | acceptance suite | world-compiler-impl / causal-adversary | CWF-8 |
| FD-41 | HIGH | **A cited-factual-resolution world receives ZERO mechanical scrutiny.** `mechanical_world_checks` short-circuits to a single PASS at `world_review.py:460-470` (`_cited_factual_resolution`, `world_compiler.py:1689`). The OPEC+ class of run therefore gets no mechanical check at all; the only remaining attack is `terminal_preresolved` in the LLM review, which `_settled_record_block` (world_review.py:718+) correctly aims at the citation's fit to the question — but that is an OPINION with no mechanical backstop, and it goes advisory under FD-34. CWF-7 must supply the mechanical branch: when a terminal is preresolved from citations, at least one reading-level check has to be a HARD gate. **PARTIALLY ADDRESSED, NOT CLOSED.** The early return no longer short-circuits to a single PASS; it keeps that PASS (the operational-depth attacks stay off — demanding a production process for a question the record already answered is what manufactured the BoE absolute NO) and appends three blocking checks on the citation itself: `cited_resolution_claims_exist` (CRITICAL — every id an `evidence:` terminal producer cites must resolve to a claim admissible at the cutoff; this is FD-30's hole at the point where it decides an answer), `cited_resolution_rests_on_the_record` (CRITICAL — at least one cited claim must be an OBSERVATION with a verified excerpt; a record that "already answered" the question is a record, not a conclusion the compiler drew), and `cited_resolution_subject_matches` (HIGH). **RESIDUAL, deliberately left open and ruled on by CTO:** the subject check is implemented only in its unambiguous limit — the cited record must share at least one significant token with SOMETHING the world names (subject, resolution units, title, any entity name, or the terminal's description), firing only when the citation has nothing whatever in common with the world it settles. Full token-matching against `subject_entity` was REFUSED because it rejects correct worlds: a world about the EU–Mercosur agreement is legitimately settled by a claim about the European Commission, and the repo's own comments cite that case. **A gate that refuses correct worlds is worse than the hole it closes.** Real scope adjudication needs the contract and the question, which `mechanical_world_checks` does not receive; threading them is a signature change that must wait until world-review-impl's lineage-closure pass lands, then belongs to CWF-7's adjudication step, not here. | world review | integration-1 + world-compiler-impl / retrieval-adversary | CWF-7, FD-8 |
| FD-1 | CRITICAL | Tesla world = cited Q2 × invented factor, zero actors, one set_field; published 0.50 was branch construction (artifacts/forensics/population) | semantic planning | world-compiler-impl / causal-adversary | CWF-2,3 |
| FD-2 | CRITICAL | Ungrounded disagreeing weights published their average as headline (BoE 0.25, Tesla 0.50) | aggregation/publication | forecast-integrity / forensic auditor | FI-2 |
| FD-3 | CRITICAL | Threshold-straddling invented alternatives (0.8/1.05 vs break-even 0.8331) undetected end-to-end | semantic validation | world-compiler-impl / causal-adversary | CWF-4, FI-5 |
| FD-4 | CRITICAL | Responsibility classification exists only in the offline forensic script; publication is ungated (a BRANCH_WEIGHTS_DOMINATED run published normally) | forecast assembly | forecast-integrity / forensic auditor | FI-3,4 |
| FD-5 (CLOSED 127f8a7, obs-reviewer) | HIGH | No per-branch initial state, state diffs, communications, or process transitions in the production trace (forensics had to derive them) | trace write | obs-trace / obs-reviewer | OBS-1..4,7 |
| FD-6 (CLOSED 127f8a7, obs-reviewer) | HIGH | Replay viewer does not render the forensic/observability artifact set | viewer | obs-viewer / obs-reviewer | OBS-6 |
| FD-7 (CLOSED 89e04c0, realism-adversary) | HIGH | BoE branch: `release_at: null` collapsed all data releases to t0; outcome decided at first invocation; 10/14 invocations causally inert; actor re-signaled 3× (run's own audit: HIGH) | semantic planning + scheduling | runtime-impl + world-compiler-impl / realism-adversary | TMP-4, ACT-8 |
| FD-8 | HIGH | OPEC+ resolution-scope ambiguity (group-wide quota vs sub-group unwinding) silently resolved; store contained both readings; published 1.00 with [1,1] bounds | resolution contract | world-compiler-impl / retrieval-adversary | CWF-7 |
| FD-9 | HIGH | Zero-actor worlds admissible without evidence that no human decision matters (Tesla passed compile with no actors) | compile gates | world-compiler-impl / causal-adversary | CWF-5 |
| FD-10 | HIGH | Uncertainty alternatives carry no meaning/sensitivity/structure-vs-state fields; planner may invent numeric alternatives around thresholds (§15 unimplemented) | semantic schema | world-compiler-impl / forecast-integrity | FI-6 |
| FD-11 | MEDIUM | `"other"`-style degenerate filler alternative carried the entire YES mass in BoE (grounding: "No specific alternative in evidence") | semantic validation | world-compiler-impl / causal-adversary | FI-6 |
| FD-12 | MEDIUM | Same-commit plan variance unmeasured/unstabilized (population: 0.50 world vs unresolved world on identical inputs) | planning determinism | world-compiler-impl / forecast-integrity | REL-3,4 |
| FD-13 (CLOSED 89e04c0, realism-adversary) | MEDIUM | Temporal metrics (§9) not computed or reported on any run | runtime reporting | runtime-impl / realism-adversary | TMP-2 |
| FD-14 | MEDIUM | branch_schedule "events" counts in-loop events only; artifact counters unreconciled with ledger (uniform +2/branch) | diagnostics | reliability-impl / chaos-adversary | REL-2 |
| FD-15 | LOW | report.md boilerplate limitation ("deleting actor calls deletes the forecast") emitted for zero-actor runs | reporting | reliability-impl / release-manager | REL-2 |
| FD-16 | LOW | branch-id `primary/` prefix inconsistency needs a joined-view helper for readers (namespacing itself fixed in b2c10ed) | trace format | obs-trace / obs-reviewer | OBS-7 |

## Closed this run (verification: 304-test suite + forensic re-run at e17f189)

| ID | Was | Fixed | Proof |
| --- | --- | --- | --- |
| FC-1 | Ledger not namespaced; two mechanical audit checks structurally vacuous, false PASS published | b2c10ed | regression + forensics re-run |
| FC-2 | Citations validated on entities only; transposed-id claim grounded a NO branch; coverage said complete | b2c10ed | fabricated-citation regression |
| FC-3 | Single-variable-only initialization check missed conjunctions (BoE) | 189c88d | conjunction regression |
| FC-4 | ForecastIntegrity computed but persisted nowhere | 189c88d | forecast.json integrity block |
| FC-5 | Call/token counters snapshotted before audit call; sealed forecast disagreed with own log | 189c88d | counter regression |
| FC-6 | Factual-resolution runs labeled weighted_simulated_trajectories | 71393bb | source-label regression |
| FC-7 | No executable world persisted; replay required an LLM | 71393bb | compiled_world.json |
| FC-8 | llm_calls carried hash-only prompts, no chronology | 71393bb | stamped prompts/timestamps |
| FC-9 | Report line crashed completed run's trace write (intent={}) | 19f2a67 | totality regression |
